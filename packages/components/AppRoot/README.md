# @hrgui/neko-terebi-react-component-app-root

Contains the app root component. The App root component is responsible for being the wrapper around React Router (think of it as the one that holds the layout)
