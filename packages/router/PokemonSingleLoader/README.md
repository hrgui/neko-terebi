# @hrgui/neko-terebi-pokemon-single-loader

Loads a single Pokemon, but only listens through events for @tanstack/react-query

# Event structure

- fetchEventName: `pokemon/fetch`
  - args - id: string
- responseEventName: `pokemon/response`
